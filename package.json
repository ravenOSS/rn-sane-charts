{
  "name": "rn-sane-charts",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "ios:start": "bash ./scripts/ios-start.sh",
    "sync:primary": "bash ./scripts/sync-primary.sh"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8"
  }
}
